# Gate 0 Core Baseline Trigger

This no-op control file exists solely to open a reviewable pull request and trigger the default-branch Gate 0 core baseline workflow.

It makes no implementation or release claim.
